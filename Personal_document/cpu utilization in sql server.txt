Fix methods:--

Methods 1:-- [traditional]
Finding query with issue with traditional methods.
Killing process or taking other necessary action required 


Method 2:--
Going 1 level deeper
using built in dynamic management view
Finding query with maximum total worker time or execution count
and optimizing it if itis select query 
By creaating index if suggestes or required
by droping index if required
by creting partitions
by changing partitions
by splitting long running update queries in smaller units

 COntact number: 9023262520
techsapphire.in
yogesh.mehla


-------------------------------------------------------------------------------------------------------------

Solution:--  we can see the cpu utilization in activity monitor
1. Tool:-   https://www.sqlshack.com/sql-server-activity-monitor/  -------> given details about activity monitor
*Sql Load generator
*Windows Task manager 

*Exec
sp_who2 (cpu time show)
sp_who
sp_whoisactive



Check dynamic management View:--

Select*from sys.dm_exec_query_stats
Select*from sys.dm_exec_query_stats order by creation_time desc
Select*from sys.dm_exec_query_stats order by toatl_worker_time desc
Select*from sys.dm_exec_query_plan (plan)
Select*from sys.dm_exec_query_procedure_stats
----------------------------------------------------------------------------------------------------------------------------------
Find top query those are facing some issue

*select top 10 * from sys.dm_exec_query_stats  q
outer apply sys.dm_exec_sql_text(q.sql_handle) d
order by total_worker_time/ execution_count desc
--------------------------------------------------------------------------------------------------------------------------------------

****Right Solution*****
Open Activity Monitor
--------------------------------------------------------------------------------------------------
Run such command:- for Cpu utilization Given some special information

sys.dm_os_tasks view
sp_who
sys.dm_exec_requests
sys.dm_os_waiting_tasks
sys.dm_exec_session
sys.dm_resource_governor_workload_groups
sys.dm_exec_requests
sys.dm_exec_query_stats
---------------------------------------------------------------------------------------------------

1. Processes Pane:- shows the information about the currently running processes on the SQL databases, who runs them, and from which application. each table header shows a short description of the column data and system views used to obtain it. 
Each column can be filtered using the filters in the column header. The context menu for the specific process provides options to see the last T-SQL command batch for the process, kill it, or trace it in SQL Server Profiler

@spid(session ID) value returned by the *sp_who* procedure
@User Process – 1 for user processes, 0 for system processes. The default filter is set to 1
@Login – the SQL Server login that runs the session
@Database – the database name on which the process is running
@Task State – the task state, blank for tasks in the runnable and sleeping state,  sys.dm_os_tasks view, as the task_state column
[“PENDING: Waiting for a worker thread.
RUNNABLE: Runnable, but waiting to receive a quantum.
RUNNING: Currently running on the scheduler.
SUSPENDED: Has a worker, but is waiting for an event.
DONE: Completed.
SPINLOOP: Stuck in a spinlock.”]
@sys.dm_exec_requests view, as the command column
@sys.dm_exec_requests view, as the command column, obtained using the sys.dm_os_waiting_tasks view, as the wait_duration_ms column
@the last/current wait type, sys.dm_os_waiting_tasks view, as the wait_type column 
@the resource the connection is waiting for, sys.dm_os_waiting_tasks view, as the resource_description column
@sys.dm_os_waiting_tasks view, as the blocking_session_id column
@sys.dm_exec_sessions view, as the memory_usage column
@sys.dm_exec_sessions view, as the host_name column
@sys.dm_resource_governor_workload_groups view, as the name column

2. Resource Wait Pane:- 
Cumulative Wait Time (sec) – the total time waiting tasks have waited for one or more resources since the last SQL Server restart, or DBCC SQLPERF last execution


3. Data file I/O pane:-
all database files are listed – MDF, LDF and NDF, their paths, and names
MB/sec Read – shows recent read activity for the database file
MB/sec Written – shows recent write activity for the database file
Response Time (ms) – average response time for recent read-and-write activity


4. Recent Epensive Queries pane:- 
@that use much resources – memory, disk, network. 
@shows expensive queries executed in the last 30 seconds
@ sys.dm_exec_requests and sys.dm_exec_query_stats views

@Query – the SQL query statement monitored
@Executions/min – the number of executions per minute, since the last recompilation
@CPU - obtained using the sys.dm_exec_query_stats view, as the total_worker_time column
@Physical Reads/sec, Logical Writes/sec, and Logical Reads/sec – the rate of physical reads/logical writes/logical reads per second. 
The value can also be obtained using the sys.dm_exec_query_stats view, as the total_physical_reads/ total_logical_writes/ total_logical_reads columns.
@Average Duration:- Calculated based on the total_elapsed_time and execution_count columns in the sys.dm_exec_query_stats view
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

01/06/2022: -->>
<<<<<-------------->>>>>>
 Although there are many possible causes of high CPU usage that occur in SQL Server, the following ones are the most common causes:
*High logical reads that are caused by table or index scans because of the following conditions:
	*Out-of-date statistics
	*Missing indexes
	*Parameter sensitive plan (PSP) issues
	*Poorly designed queries
*Increase in workload

CPU is consumed in two different modes:

            1) User Mode

           2) Kernel Mode



=====================================================================================================================================================
------SQL Server CPU Utilization history report for last N minutes:

DECLARE @ts BIGINT;
DECLARE @lastNmin TINYINT;
SET @lastNmin = 60;
SELECT @ts =(SELECT cpu_ticks/(cpu_ticks/ms_ticks) FROM sys.dm_os_sys_info); 
SELECT TOP(@lastNmin)
		SQLProcessUtilization AS [SQLServer_CPU_Utilization], 
		SystemIdle AS [System_Idle_Process], 
		100 - SystemIdle - SQLProcessUtilization AS [Other_Process_CPU_Utilization], 
		DATEADD(ms,-1 *(@ts - [timestamp]),GETDATE())AS [Event_Time] 
FROM (SELECT record.value('(./Record/@id)[1]','int')AS record_id, 
record.value('(./Record/SchedulerMonitorEvent/SystemHealth/SystemIdle)[1]','int')AS [SystemIdle], 
record.value('(./Record/SchedulerMonitorEvent/SystemHealth/ProcessUtilization)[1]','int')AS [SQLProcessUtilization], 
[timestamp]      
FROM (SELECT[timestamp], convert(xml, record) AS [record]             
FROM sys.dm_os_ring_buffers             
WHERE ring_buffer_type =N'RING_BUFFER_SCHEDULER_MONITOR'AND record LIKE'%%')AS x )AS y 
ORDER BY record_id DESC;


==============================================================================================================================





















